<br>
<% if user_signed_in? %>
	<div class="list-group">
		<span class="list-group-item active">
			<h4><b>Categories</b></h4>
			<% Degre.all.each do |degree| %>
		</span>
				<%= link_to degree.name_with_type, degree, class: "list-group-item" %>
			<% end %>
	</div>
<% end %>
