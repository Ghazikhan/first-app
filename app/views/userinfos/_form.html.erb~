<hr>
<p class="alert alert-info">
Note..! Fill these field very carefully. After submit will not be editable.
</p> 
<%= form_for([@user, @user.userinfos.build]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="row">
  <div class="col-md-8">
    <%= f.date_select(:date_of_birth, start_year: 1970, order: [:day, :month, :year], 
    																	prompt: { day: 'Select day', month: 'Select month', year: 'Select year' }) %>
  </div>
  </div>

 <div class="row">
	<div class="col-md-1">
		  <%= f.radio_button :gender, "Male" %>
	 </div>
   <%= f.label :gender, "Male" %>
 </div>
 <div class="row">
	 <div class="col-md-1">
		  <%= f.radio_button :gender, "Female" %>
	 </div>
    <%= f.label :gender, "Female" %>
 </div>
 <div class="row">
	 <div class="col-md-4">
		  <%= f.text_field :mobile, class: "form-control", placeholder: "Phone" %>
		</div>
  </div>
    <%= f.text_area :address, class: "form-control", placeholder: "Address" %>

    <%= f.submit "Submit", class: "btn btn-primary" %>

<% end %>
