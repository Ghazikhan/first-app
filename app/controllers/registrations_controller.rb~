class RegistrationsController < Devise::RegistrationsController

  protected

    def after_update_path_for(resource)
    	if @user.update(resource)
      	user_path(resource)
      else
      	render @user
      end
    end
end
